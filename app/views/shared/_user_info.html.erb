<%= link_to gravatar_for(current_user,
                         Settings.app.size_gravatar),
                         current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_my_profile"), current_user %></span>
<span>
  <%= t("shared.user_info", count:current_user.microposts.count) %>
</span>
